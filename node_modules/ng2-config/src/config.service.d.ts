import { Http } from '@angular/http';
import 'rxjs/add/operator/map';
import 'rxjs/add/operator/toPromise';
export declare abstract class ConfigLoader {
    abstract getApiEndpoint(): string;
}
export declare class ConfigStaticLoader implements ConfigLoader {
    private apiEndpoint;
    constructor(apiEndpoint?: any);
    getApiEndpoint(): string;
}
export declare class ConfigService {
    private http;
    loader: ConfigLoader;
    private settingsRepository;
    constructor(http: Http, loader: ConfigLoader);
    init(): any;
    getSettings(group?: string, key?: string): any;
}
